import styled from 'styled-components';

export const ThemeSelectorStyle = styled.section`
  cursor: pointer;
  display: grid;
  grid-auto-flow: column;
  align-items: center;
  justify-content: center;
  gap: 0.5em;
`;
